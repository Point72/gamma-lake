## Pre-requisites

You need Python >=3.10 on your machine to install `gamma-lake`.

## Install with `pip`

```bash
pip install gamma-lake
```

This installs Gamma Lake for local execution without Ray. To enable distributed execution:

```bash
pip install "gamma-lake[ray]"
```

## Install with `conda`

```bash
conda install gamma-lake --channel conda-forge
```

## Source installation

For other platforms and for development installations, [build `gamma-lake` from source](Build-from-Source).
